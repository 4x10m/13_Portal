# ── AxiiomLab Dashboard — Environment config ──
# Copy to .env.local for local dev: cp .env.example .env.local

DATABASE_PATH=./data/dashboard.db
OPENCODE_DB_PATH=/home/debian/.local/share/opencode/opencode.db
CODEBASE_DIR=/home/debian/Codebase
CLOUD_MANAGER_SCRIPT=/home/debian/Codebase/1_infra/1_cloud_manager/cloud_manager.sh

# Optional (for Docker compose — already in root .env)
# TAILSCALE_API_KEY=...
# TAILNET=...
# PG_HOST=host.docker.internal
# PG_PORT=5432
